export type RegistrationFormData = {
  userName: string;
  email: string;
  contact: string;
  password: string;
  role: string;
};

export type RegistrationListItem = {
  id: number;
  userName: string;
  email: string;
  contact: string;
  role: string;
};
